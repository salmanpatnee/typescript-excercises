function make_shirt(size: string, message: string): string {
    return `Size of the shirt is ${size} and "${message}" message is printed on the shirt`;
}

console.log(make_shirt("Large", "Catch me if you can!"));
